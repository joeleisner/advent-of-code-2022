// Sorts numbers in ascending order (smallest to largest)
export const ascending = (previous: number, next: number) => previous - next;

// Sorts numbers in descending order (largest to smallest)
export const descending = (previous: number, next: number) => next - previous;
